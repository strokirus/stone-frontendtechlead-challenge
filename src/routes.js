import Home from './routes/Home.html';
import Character from './routes/Character.html';
import Settings from './routes/Settings.html';
import History from './routes/History.html';

export default {
  '/': Home,
  '/character': Character,
  '/history': History,
  '/settings': Settings,
};
